Use Coverage.collect and Coverage.Report.write as my examples
Dynamic import question for import * as Module from "file";


instead of kind: , it could be an instance call with new or .build() then I wont need deriveBuildErrorType and formatBuildErrors neither
many static methods under 'setup/run-state.ts'


● Looked through the README, CLI flags, TAP output, and existing keyboard/watcher infra. Here are the ones I'd argue clear the "significant UX win" bar — ranked by how much they actually change the day-to-day:
  3. Watch-mode interactive hotkeys (Vitest-style)

  The infrastructure exists (lib/setup/keyboard-events.ts, lib/utils/listen-to-keyboard-key.ts, ~100 LOC total) — only the bindings are missing. f = only failed, t = prompt for
  test-name regex, p = prompt for file glob, a = all, o = open output, q = quit. Turns --watch from "rerun on save" into an interactive REPL for the suite. Highest ratio of
  impact-to-implementation-cost on the list.


- investigate take screenshot feature & visual diffing(implement it percy-like)

- Think about error handling in JS. Maybe find a better solution than shellFails. check lua's error handling for JavaScript on all my JS code, if this is a better way to do error handling:
Given this conversation: https://chatgpt.com/share/69e26e08-4dbc-83eb-8d7d-01bba54c8198 I want to create a general-purpose error handling system for JS/TS. What would be the best way to achieve this for the best general-purpose, very useful, readable, performant and flexible abstraction. Show me in code, in-depth with clear tutorial at the end mention all the corner cases, everything, I suspect it is a good idea, I'm still evaluating but would love a general-purpose solution for better error-handling

- enhance flake.nix and README.md

- Standardize function naming of onCustomFunction, all functions should follow a naming standard, currently it seems 
the worst by having:
order imports according to some intutive logic to make them more readable
make all .catch(() => {}) print something to debug
move helper functions to below

Maybe move BundleError to ./errors directory

- refactor and optimize: run/tests-in-browser(and run.ts, very complex), setup/browser.ts, setup/file-watcher.ts

- Test and optimize Dockerfile

Also optimize someFuncCall();return; to return someFuncCall() to save lines whenever possible. 

