- is stdout during qunitx and its test suite runs, runs in streaming fashion for the ultimate user and developer 
experience and speed to make everything feel real-time? If not, check to see if it can be optimized, explain it to me and we can optimize it if needed

qunitx-cli JS API should be very usable from outside by JS/TS only, by users of this library. Do them, document it well
in the codebase and also on the README.md(if needed).

https://www.youtube.com/watch?v=SaZEdiV3A3M

- Shouldnt shims/deno/vendor-types folder be generated by deno?

- file-watcher.js: Make an index.html that displays build errors in browser (watch mode); requires deriving error type from the caught error

pass timezone: https://stackoverflow.com/questions/16448754/how-to-use-a-custom-time-in-browser-to-test-for-client-vs-server-time-difference/39533934#39533934
= qunitx --timezone="US/Pacific"

- Think about error handling in JS. Maybe find a better solution than shellFails. check lua's error handling for JavaScript on all my JS code, if this is a better way to do error handling.

- investigate take screenshot feature & visual diffing

- Standardize function naming of onCustomFunction, all functions should follow a naming standard, currently it seems 
the worst by having:

- enhance flake.nix and README.md

- refactor and optimize: run/tests-in-browser, setup/browser.ts, setup/file-watcher.ts, setup/test-file-paths.ts

- rename: utils/read-boilerplate.ts

- check if there are any variables we can remove and/or rename on lib/types.ts

Given the existing benchmarks, is there any other important one I should add and then more importantly is there any
opportunities to optimize/refactor the runtime of my tests with this binary(qunitx)/project for the end user when
they run their suite of tests or a test?

--watch tests are slow because I've a suspicious node still prefers single thread over fs calls. It does distribute them
across cores but that thread pool doesnt get utilized or doesnt utilize all my cores and machine resources effectively.
Am I correct with my assumption?

--refactor the `test/helpers/before-script-async.ts` while keeping a similar behavior(or at least similar use when the file is used in tests it should be a backend async service I suspect that we verify in tests it gets loaded correctly) so I can remove express and cors devDependencies from my project. Also rewrite expressApp name to something correct now in my runtime, since I dont use express anymore, should it be `webServer` instead?
