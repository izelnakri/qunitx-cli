// This file intentionally contains no QUnit test registrations.
// Used by the "no tests registered" feature test to verify that running qunitx
// on a file with no test() / module() calls produces a warning (not a failure).
export const x = 1;
